<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:foreground="?attr/selectableItemBackgroundBorderless"
    app:cardCornerRadius="@dimen/card_radius_small">

    <LinearLayout
        android:padding="@dimen/padding_tiny"
        android:layout_width="match_parent"
        android:layout_height="@dimen/lookup_item_height"
        android:background="@android:color/white"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imvProduct"
            android:layout_width="@dimen/lookup_item_height"
            android:layout_height="@dimen/lookup_item_height"
            app:srcCompat="@mipmap/ic_launcher" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="@dimen/padding_small"
            android:paddingRight="@dimen/padding_small">

            <TextView
                android:textSize="@dimen/text_size_small"
                android:id="@+id/tvName"
                android:textStyle="bold"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="2"
                android:paddingLeft="@dimen/padding_small"
                android:paddingTop="@dimen/padding_small"
                android:textColor="@color/text_dark"
                android:paddingRight="@dimen/padding_normal"
                android:text="Striped Maxi Dress Color - Show your of  your life - SKU002" />


            <TextView
                android:id="@+id/tvPrice"
                android:layout_width="match_parent"
                android:textSize="@dimen/text_size_small"
                android:layout_height="match_parent"
                android:layout_below="@id/tvName"
                android:layout_marginLeft="@dimen/padding_small"
                android:gravity="center_vertical"
                android:textColor="@color/text_blue"
                android:text="Đơn giá: 40.000 VNĐ" />

            <ImageView
                android:id="@+id/btnCart"
                android:layout_width="37dip"
                android:paddingBottom="@dimen/padding_tiny"
                android:paddingEnd="@dimen/padding_tiny"
                android:paddingStart="@dimen/padding_big"
                android:paddingTop="@dimen/padding_big"
                android:layout_height="37dip"
                android:layout_alignParentEnd="true"
                android:layout_alignBottom="@+id/tvPrice"
                android:layout_centerVertical="true"
                app:srcCompat="@drawable/ic_cart" />

        </RelativeLayout>
    </LinearLayout>
</android.support.v7.widget.CardView>